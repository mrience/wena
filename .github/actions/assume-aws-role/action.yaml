name: Assume AWS Role
description: Assume an AWS Role. Defaults to the @GithubOidc role in the Identity account.
inputs:
  aws-region:
    required: false
    default: eu-west-1
    description: "The AWS region to use."
  aws-account-id:
    required: false
    default: 586808671648
    description: "The AWS account ID to assume the role in."
  role-name:
    required: false
    default: "@GithubOidc"
    description: "The name of the role to assume. Defaults to @GithubOidc."

runs:
  using: composite
  steps:
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@e7f100cf4c008499ea8adda475de1042d6975c7b #v6.2.0
      with:
        role-to-assume: arn:aws:iam::${{ inputs.aws-account-id }}:role/${{ inputs.role-name }}
        aws-region: ${{ inputs.aws-region }}